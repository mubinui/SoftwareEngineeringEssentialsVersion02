package JavaCore.FinalFinallyFinalize;

public class FinallyFinalize {
    public static void main(String[] args) {
        //Final is a keyword
        //Finally is a block that executes whatever happens
        //Finalize is a method (Used to perform clean-up processing just before
        //the object is garbage collected
        //Final class cannot be inherited . Final method cannot be overridden.

    }
}
